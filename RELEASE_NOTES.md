# madi v0.2.0

릴리즈는 전체 회귀와 새 서비스 이미지의 재반입·폐쇄망 검증을 통과한 뒤 게시합니다. 실제 검사·게시 상태는 [검증 장부](https://github.com/hkjang/madi/blob/main/OPERATIONS_UPGRADE.md)와 [배포 검증 기록](https://github.com/hkjang/madi/blob/main/docs/deployment-verification.md)에서 확인하세요. 게시 파일의 정확한 크기와 SHA-256은 릴리즈 워크플로가 이 본문 하단에 기록하며, 이 게시 정책 자체가 검사 통과나 게시 완료를 뜻하지는 않습니다.

한국어 중심의 자체 호스팅 Knowledge & AI Workspace입니다. Go 서버가 React 화면을 함께 제공하며 외부 PostgreSQL과 영구 파일 볼륨을 사용합니다. P0~P3를 제공한 v0.1.0 이후, 작성·검색·이관·AI 근거와 실제 운영 경계를 연결한 업데이트입니다. 게시 워크플로의 빌드·검증·오프라인 실행 검사를 통과한 이미지만 첨부합니다.

## 이번 업데이트

- **안심하고 작성:** 공동 편집 증분 기록·체크포인트·편집 세대 전환, 복구 필요 상태와 서버 저장 완료 구분, PostgreSQL 변경 알림, 문서별 협업 진단. 오래된 오프라인 변경은 새 세대에 무조건 병합하지 않습니다.
- **빠르게 찾기:** 한국어 띄어쓰기 정규화·조직 용어 사전·합성 평가셋·검색 해석·결과 묶기·커서·실행 진단, 권한을 적용한 정확/근사 벡터 검색 검증과 색인 세대 전환.
- **자료를 지식으로:** PDF·DOCX·PPTX·XLSX 본문 추출, 선택 페이지 영어·한국어 OCR, 원본과 위치 비교·오류 신고·검색·첨부 구간 인용. 관리자가 켠 경우에만 추출하며 원본 파일을 덮어쓰지 않습니다.
- **재개 가능한 이관:** 해시·청크·체크포인트·비공개 준비 영역, 변환 품질·전후 비교·CSV 타입 검토·중복 방지·원자 반영·왕복 시험. 큰 파일을 한 번에 올리는 기존 경로의 한도를 단순 확대하지 않았습니다.
- **AI 근거와 에이전트:** 버전 고정 근거 보관함, 토큰 예산·필수 정책·포함 이유·최신성·현재 권한을 확인하는 지식 패키지, 변경 영향과 예외 검토, 변경 제안서와 정본 병합, 업무 유효기간 검색, 서명된 망간 지식 배포.
- **문서에서 업무로:** 운영 현황 카드·명시적 출처, 제한된 선언형 문서 조회, 담당자·검토일·근거를 갖춘 공식 답변, 문서 간 모순 후보 검토, 문서 선택 구간에서 DB 행 초안 만들기, 역할별 지식 경로와 개인 진행.
- **작업 중심 UX:** 가져오기 진입점 통합, 목적별 메뉴·고정 도구, 즉시 개인 메모·내 처리함, 한글 태그 칩·문맥 패널·검색 미리보기, 선택 AI 비교·부분 반영, 공유·이동 영향 확인, DB 인라인·범위 붙여넣기·개인 보기, 모바일 하단 행동·작업 묶음·참고 선반.
- **OIDC 이메일 검증 선택:** 관리자 SSO 설정의 `oidc_require_verified_email`은 기본 `false`이며 기존 설정에 항목이 없는 업그레이드도 같습니다. `true`로 켜면 검증된 이메일을 로그인 필수 조건으로 요구합니다. 토큰 검증과 기존 계정의 명시적 연결 보호는 유지합니다.

AI 요약·모순·변경 영향은 제안이며 해시 일치가 사실성을 인증하지 않습니다. 현재 ACL은 근거·패키지·MCP·첨부·변경안에도 적용합니다. 이미 사용자가 명시적으로 내보낸 사본을 이후 권한 변경으로 회수할 수 있다고 보장하지 않습니다. 조직 승인 절차는 적용 가능한 관리자 정책이 있을 때만 사용합니다.

상세 사용법과 한도는 [전체 매뉴얼](https://hkjang.github.io/madi/manuals.html), 검증 조건과 진행 기록은 저장소의 `OPERATIONS_UPGRADE.md`를 확인하세요. 자동 브라우저 시험은 실제 사람 사용성 관찰이나 운영체제 IME 시험을 대신하지 않습니다.

`main-BPw-YbqO.js` 후보의 로컬 검증은 shared 25개 묶음 전부 통과(327.867초), 실제 정본 저장과 목록 갱신의 지연·실패를 분리한 문서 경합 전체 5회 통과(69.648초)를 확인했습니다. DB 편집 시험도 실제 저장 응답을 확인하도록 보강해 PostgreSQL 17·18에서 통과했습니다. 이전 후보의 호환성·교차 검사와 두 차례 main CI 실패 기록은 장부에 구분해 보존합니다. 수정 후 전체 Go `-race`·35개 독립 브라우저 옵션의 main CI, 새 이미지·재반입·공개 릴리즈는 별도 관문이며 로컬 통과가 이를 대신하지 않습니다.

## 최종 검증

태그 `v0.2.0`은 커밋 `aff3ee1f6d48f895b0d1461dafdf2711638eb0ce`에 고정됐으며 [main CI](https://github.com/hkjang/madi/actions/runs/34363815607)와 [릴리즈 검증](https://github.com/hkjang/madi/actions/runs/34369441095)을 모두 통과했습니다. 전체 Go `-race`·독립 브라우저 검사·6개 호환성 조합·shared25·이미지 저장/재반입·폐쇄망 실행과 데이터 복구를 확인했습니다. 게시 파일을 별도로 다운로드해 gzip·단일 이미지 태그·크기·SHA-256을 검사했고 GitHub digest 및 게시된 릴리즈 본문의 값과 일치했습니다.

## 업그레이드 주의

기동 시 스키마를 자동 갱신합니다. **교체 전에 PostgreSQL·모든 첨부 저장소·ENCRYPTION_KEY·기존 이미지를 같은 복구 계획으로 보관**하고, 복제한 환경에서 먼저 시험하세요. 변경된 DB에 예전 바이너리만 다시 올리는 방법을 롤백으로 사용하지 마세요. 복구에는 이전 버전과 그 버전의 백업을 함께 사용합니다.

OIDC 이메일 검증 요구가 꺼져 있어도 유효한 이메일과 subject, 서명·issuer·audience·nonce·state·PKCE·만료 검증은 필요합니다. 실제 `email_verified` 값을 계정 연결에 적용하므로 미검증 이메일로 기존 로컬 일반 사용자나 관리자를 자동 연결하지 않습니다. 신규 생성은 별도 자동 등록 설정에 따르며, 동일 이메일 충돌은 관리자가 제공자·issuer·`sub`를 확인해 명시적으로 연결해야 합니다. 이메일 검증을 필수로 운영하는 조직은 업그레이드 시 `oidc_require_verified_email=true`를 명시하세요. 운영 중인 Keycloak 서버를 이 변경으로 수정하거나 배포한 것은 아닙니다.

새 영구 정책·처리 이력·근거 메타데이터를 백업에 포함합니다. 검색·벡터·첨부 본문 추출처럼 재생성 가능한 파생 테이블은 논리 백업에서 제외하며 native 복원 뒤에도 폐기하거나 무효화합니다. 복원 후 세션·자동 외부 실행·서명/신뢰·AI 색인 전송 동의 등의 상태는 기능별로 폐기하거나 재확인 상태로 전환합니다. 외부 색인은 과거 동의만으로 자동 재전송하지 않습니다. 첨부 추출과 OCR은 기본 비활성화이며 **Linux Landlock ABI 3 이상·seccomp**가 필요합니다. 격리 적용 실패 시 약한 경로로 자동 전환하지 않습니다.

## 제공 범위

- 워크스페이스, Markdown 문서와 블록 ID·다중 선택·이동, 위키 링크·백링크·그래프, 문서 트리·검색·즐겨찾기·할 일
- 문서 버전·휴지통 보존기간 정리·첨부파일, 데이터베이스 표·보드·캘린더, Vault 폴더·첨부를 보존하는 Markdown ZIP 입출력
- 로컬 로그인·Keycloak OIDC, 역할과 문서 권한, 개인화 및 분리된 관리자 화면
- 워크스페이스·권한·만료·IP·호출 제한을 설정하는 API 키와 즉시 키 회전
- REST API·OpenAPI 목록·HTTP MCP, OpenAI 호환 스트리밍 AI 및 최대 262,144 토큰 설정
- 관리자 사용자·설정·이력·감사와 논리 백업·복원, 설정할 때만 사용하는 승인 절차
- 한국어 반응형 화면, 로고·파비콘, 로그인·프로필 메뉴의 서비스 버전
- 오프라인 서비스 이미지, Compose·Kubernetes 예제, 제품 소개와 사용자·관리자·운영 가이드
- Yjs 실시간 협업·커서·presence, 고급 중첩·동기화·임베드 블록·KaTeX·Mermaid·다단 편집
- 데이터베이스 6개 보기·관계·안전 수식·롤업·AI 속성, 팀 할 일·칸반·달력·토론·멘션·알림
- 문서 소유권·생명주기·보존·품질·지식 건강, 작업 큐·outbox·Webhook·자동화
- 동의 기반 임베딩·하이브리드 RAG·재정렬·현재 원문 인용, 자연어 검색·선택 문서 요약·개인 AI/검색 기록
- AI 그래프·엔터티·주제·중복·지식 보완 후보와 사용자 단건 확인, Workspace Agent의 도구 제한·쓰기 확인
- SAML·TLS LDAP·SCIM·그룹 매핑, 1/N단계·부서·병렬 승인, 읽기 전용 지원 진단
- 기업 문서 커넥터·외부 SQL 소스·Entity·읽기 전용 Text2SQL, 고정 원격 Git 동기화
- 민감정보 탐지·마스킹·분류·워터마크·보안 공개 공유·법적 보존
- Canvas·Drawing·격리 Plugin SDK, Tauri·PWA·오프라인 개인 보관함·Web Clipper
- 격리·권한·확인 기반 실행형 Runbook, 운영·오류·OpenTelemetry·기능 플래그·워크스페이스 브랜딩

## 배포

검증을 통과해 게시된 릴리즈 자산은 `madi:v0.2.0` 이미지를 저장한 `madi-v0.2.0.tar.gz` 하나입니다. 기본 플랫폼은 Linux amd64입니다. PostgreSQL은 별도로 준비하며 릴리즈에 제3자 서비스 이미지는 포함하지 않습니다. PDF/OCR 도구·영어/한국어 모델·로컬 웹 자산·런타임 구성 요소의 대응 소스와 고지를 같은 이미지에 넣도록 빌드하며, 최종 이미지에서 다시 검증합니다. 소스·체크섬·PostgreSQL 파일을 추가 자산으로 올리지 않습니다.

```sh
gzip -dc madi-v0.2.0.tar.gz | docker load
```

서비스 실행에는 `POSTGRES_DSN`, `BOOTSTRAP_ADMIN`, `BOOTSTRAP_ADMIN_PASSWORD`, `ENCRYPTION_KEY` 네 환경변수가 필요합니다. 그 밖의 운영 설정은 관리자 화면에서 관리합니다. ENCRYPTION_KEY는 32바이트 무작위 값의 Base64 표현이며 별도 안전한 장소에 백업해야 합니다.

## 범위와 제한

기존 P0~P3 기능에 운영·UX 개선을 연결한 업데이트입니다. 각 기능은 관리 정책·현재 권한을 적용하며 선택 외부 인프라는 별도 연결합니다. 프로토콜 시험을 실제 조직 자격증명 연결 시험으로 확대 해석하지 않습니다. Linux Tauri와 Windows·macOS 실행 검증, HTTP MCP와 stdio 전송을 구분합니다. Notion·Obsidian의 모든 독자적 기능을 무손실 변환한다고 보장하지 않습니다. [전체 기능 범위와 운영 한계](https://hkjang.github.io/madi/manuals/roadmap.html)를 확인하세요.

관리자 백업 ZIP은 같은 서비스 버전과 ENCRYPTION_KEY를 사용하는 환경에서 복원할 수 있습니다. 복원은 현재 데이터를 교체하고 세션을 폐기하므로 먼저 격리 환경에서 시험하세요. ZIP 256MB·압축 해제 1GB·10,000개 항목으로 제한하며 대규모 복구와 PITR에는 PostgreSQL·첨부파일의 같은 시점 백업을 사용합니다. 대규모 처리량과 지연 시간은 별도의 부하 시험 전에는 보장하지 않습니다.

릴리즈 워크플로는 웹·Go 빌드, Go 검사, 이미지 재반입 및 외부 통신이 차단된 Docker 네트워크에서 준비 상태·로그인 기동 검사를 통과한 뒤 이미지를 게시합니다. 실제 운영망의 TLS·DNS·프록시·IdP·AI 호환성은 운영 환경에서 추가로 확인하세요.

2026-09-09 By2 후보의 최종 로컬 검사에서 Go 호출 경로·가져오는 패키지 취약점은 0건, 웹 프로덕션 의존성 취약점은 0건이었습니다. 의존하는 모듈에는 애플리케이션에서 호출되지 않은 취약점 1건이 남아 있습니다. `go vet`·서식 검사, Node 26개 검사와 라이선스 423개 검증도 통과했습니다. 이는 새 배포 이미지의 검사를 면제하지 않습니다. 2026-09-08에 별도로 검사한 데스크톱 Rust SDK에는 GTK 계열의 정보형 경고 17건이 남아 있으며, 해당 SDK는 서비스 Docker 런타임에 포함되지 않습니다. 현재 확인된 입력 경계와 경고의 한계는 [보안 검증 안내](https://hkjang.github.io/madi/manuals/security-verification.html)에 명시합니다.
